'use client'
import { useQuery } from 'react-query'

export default function Home() {
  // const {data, isLoading, isError} = useQuery('todos', () => fetch('https://jsonplaceholder.typicode.com/todos').then(res => res.json()))
  // return (
  //   <main className="flex min-h-screen flex-col items-center justify-between p-24">
  //     {data && data.map((todo: any) => <div key={todo.id}>{todo.id}</div>)}
  //   </main>
  // )
}
